@model List<LogisticsTrackingSystem.Models.Shipment>

@{
    ViewData["Title"] = "Archived Shipments";
}

<h2>Archived Shipments</h2>

<table class="table table-bordered">

    <thead>
        <tr>
            <th>Id</th>
            <th>Shipment Code</th>
            <th>Tracking Number</th>
            <th>Destination</th>
            <th>Status</th>
        </tr>
    </thead>

    <tbody>

    @foreach (var shipment in Model)
    {
        <tr>

            <td>@shipment.Id</td>
            <td>@shipment.ShipmentCode</td>
            

            <td>@shipment.TrackingNumber</td>

            <td>@shipment.Destination</td>
            <td>@(shipment.Status ?? "Pending")</td>
              
        </tr>
    }

    </tbody>

</table>